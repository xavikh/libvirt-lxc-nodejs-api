<volume>
    <name>{{name}}</name>
    <allocation unit="G">{{size}}</allocation>
    <capacity unit="G">{{size}}</capacity>
    <target>
        <path>/dev/centos/{{name}}</path>
    </target>
</volume>